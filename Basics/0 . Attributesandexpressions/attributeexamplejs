({
doInit : function(component, event, helper) {
        console.log('inside doinit');
var numbers =[];
        for(var i=0;i<10;i++){
            numbers.push({
                value:i
            });           
        }
         console.log('inside numbers'+numbers);
        component.set("v.intlist",numbers);
}
})