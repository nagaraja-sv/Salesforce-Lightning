({
getMessageHandler : function(component, event, helper) {
    var params = event.getParam('arguments');
    if(params){
        var namearg = params.Name;
        return "hello"+namearg+"From Child Component"
    }
}
})